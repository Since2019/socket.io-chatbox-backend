{
  "name": "react-chatbox-backend",
  "version": "1.0.0",
  "description": "A Node.js backend for socket.io chatbox ",
  "main": "server.js",
  "scripts": {
    "test": "npm run dev",
    "dev": "ts-node-dev --respawn --transpile-only server.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
